apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-appset
  namespace: argocd
  labels:
    custom.kubernetes.io/is-adhoc: "true"
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - matrix:
        generators:
          - git:
              repoURL: https://github.com/joshua-nixon/nixon-gitops.git
              revision: main
              files:
                - path: "argocd/workloads/cluster-workload.yaml"
          - list:
              elementsYaml: "{{ .applications | toJson }}"
  template:
    metadata:
      name: "{{ .name }}"
      annotations:
        argocd.argoproj.io/manifest-generate-paths: "/charts/nixon-cronjob;/argocd/workloads/cluster-workload.yaml;/configuration/base/values.yaml;/configuration/cluster/{{ .name }}.yaml"
    spec:
      project: default
      source:
        repoURL: https://github.com/joshua-nixon/nixon-gitops.git
        targetRevision: main
        path: "{{ .chart }}"
        helm:
          releaseName: "{{ .name }}"
          ignoreMissingValueFiles: true
          valueFiles: ["/configuration/base/values.yaml", "/configuration/cluster/{{ .name }}.yaml"]
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .namespace }}"
      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/replicas
      syncPolicy:
        managedNamespaceMetadata:
          labels:
            custom.kubernetes.io/add-image-pull-secret: "true"
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - Prune=true
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
          - RespectIgnoreDifferences=true
